# Plot method for cv.hierNest objects

Plot method for cv.hierNest objects

## Usage

``` r
# S3 method for class 'cv.hierNest'
plot(x, type = c("coefficients", "Subgroup effects"), ...)
```

## Arguments

- x:

  An object of class `cv.hierNest`

- type:

  Plot type, e.g. "fit" for observed vs fitted (default), others you may
  add later (e.g., "coef").

- ...:

  Other parameters

## Value

Invisibly returns `x`
